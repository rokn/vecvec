# Loads the Nix flake dev shell (default = stable Rust toolchain).
# Requires direnv + nix-direnv. Run `direnv allow` once to trust this file.
use flake

# Optional: enable the sccache compile cache for faster rebuilds.
# export RUSTC_WRAPPER=sccache
